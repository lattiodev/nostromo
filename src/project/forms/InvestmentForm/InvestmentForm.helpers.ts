/**
 * Returns the default values for the investment form.
 *
 * @returns {Object} An object containing default values for all investment form fields.
 * @property {number} amount - The default investment amount, set to 0.
 */
export const getDefaultInvestmentFormValues = () => ({
  amount: 0,
});
